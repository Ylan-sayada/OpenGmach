export enum ACCOUNTANCY_DEF {
    INCOME, OUTCOME
}
export enum SCHEDULE_IMPORTANCE {
    LOW, MEDIUM, HIGH
}
export enum ENUM_MANAGE {
    SHOW = "show", CREATE = "create", REMOVE = "remove", UPDATE = "update"
}